// src/redux/reducers.js
import { combineReducers } from 'redux';
// Importa tus reducers aquí

const rootReducer = combineReducers({
  // tus reducers aquí, por ejemplo:
  // user: userReducer,
  // posts: postsReducer
});

export default rootReducer;
